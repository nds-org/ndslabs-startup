apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ndslabs-ingress
spec:
  tls:
  - hosts:
    - www.lambert.ndslabs.org
    secretName: ndslabs-tls-secret
  rules:
  - host: www.lambert.ndslabs.org
    http:
      paths:
      - path: /api
        backend:
          serviceName: ndslabs-apiserver
          servicePort: 30001
      - path: /ui
        backend:
          serviceName: ndslabs-gui
          servicePort: 80
